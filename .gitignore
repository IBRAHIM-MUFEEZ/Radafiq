*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# Firebase config files (contain API keys)
app/google-services.json
web-app/src/firebase.ts
web-app/.env
web-app/.env.local

# Firebase Admin service account key — NEVER commit this
web-app/serviceAccountKey.json

# Claude Code local config (per-project settings, memory, plans)
.claude/
.opencode/

# Session artifacts
diff_*.txt
gradle_err.txt
gradle_out.txt
prompt.txt

# Separate unrelated project (Open Design)
open-design/

# Release signing keystore — NEVER commit this
*.jks
*.keystore
# Exception: shared debug keystore for consistent SHA-1 across machines
!app/radafiq-debug.jks
